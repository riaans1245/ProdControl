@model IReadOnlyCollection<test1233.Models.ContactUs>

@{
    ViewData["Title"] = "Contactus Report";
    Layout = "~/Views/Shared/_ReportLayout.cshtml";
}

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h3 mb-1">Contact us Report</h1>
            <p class="text-muted mb-0">Current Contact-Us info available in the system.</p>
        </div>
        <a asp-action="ExportCategories" class="btn btn-success">Export to Excel</a>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped align-middle mb-0">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Name</th>
                            <th>Surname</th>
                            <th>Cell No</th>
                            <th>Email Address</th>
                        </tr>
                    </thead>
                    <tbody>
                    @foreach (var contactus in Model)
                    {
                        <tr>
                            <td>@contactus.Id</td>
                            <td>@contactus.Name</td>
                            <td>@contactus.Surname</td>
                            <td>@contactus.CellNo</td>
                            <td>@contactus.EmailAddress</td>
                        </tr>
                    }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>