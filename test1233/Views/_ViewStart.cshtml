@{
    var isAdmin = User.IsInRole("Admin");
    var isUser = User.IsInRole("Users");
    
    if (isAdmin)
        {
            Layout = "_Layout";
        }
    if (User.Identity?.Name == null || isUser)
        {
            Layout = "_LayoutUsers";
        }
}
